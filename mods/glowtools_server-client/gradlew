#!/bin/sh

#
# Copyright 2015 the original author or authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

##############################################################################
##
##  Gradle start up script for POSIX generated by Gradle.
##
##  Important for running:
##
##  (1) You need a POSIX-compliant shell to run this script. If your /bin/sh does
##      not find it, but has a "bash" or "ksh" that DOES find it, you may need to
##      have the BUILD_SHELL environment variable (set to "bash" or "ksh") and have
##      "/bin/sh" run something like: "exec bash $0" or "exec ksh $0"
##
##  (2) The local .gradle directory is treated as your personal gradle user home.
##      Therefore, unlike /etc/gradle, it is used for 3rd party plugins and caches.
##
##############################################################################

# Add default JVM options here. You can also use JAVA_OPTS and GRADLE_OPTS to pass JVM options to this script.
DEFAULT_JVM_OPTS='"-Xmx64m" "-Xms64m"'

# Use the maximum available, or set MAX_FD != unlimited.
MAX_FD=maximum

warn() {
    echo "$*" >&2
}

die() {
    echo
    echo "$*"
    echo
    exit 1
}

cygwin=false
msys=false
darwin=false
nonstop=false
case "$(uname)" in
CYGWIN*) cygwin=true ;;
Darwin*) darwin=true ;;
MINGW*) msys=true ;;
NONSTOP*) nonstop=true ;;
esac

CLASSPATH=$APP_HOME/gradle/wrapper/gradle-wrapper.jar

if ! command -v java &> /dev/null; then
    die "ERROR: JAVA_HOME is not set and no 'java' command could be found in your PATH."
fi

JAVA_EXE="java"
JAVA_OPTS=(-XX:+IgnoreUnrecognizedVMOptions -XX:+UseG1GC -XX:G1NewCollectionPercentage=20 -XX:G1MaxNewGenPercentSize=30 -XX:InitiatingHeapOccupancyPercent=35 -XX:+DisableExplicitGC -Dfile.encoding=UTF-8 -Duser.country=US -Duser.language=en -XX:+TieredCompilation -XX:TieredStopAtLevel=4)

exec "$JAVA_EXE" "${JAVA_OPTS[@]}" -classpath "$CLASSPATH" org.gradle.wrapper.GradleWrapperMain "$@"
